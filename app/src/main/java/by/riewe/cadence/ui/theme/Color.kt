// by/riewe/cadence/ui/theme/Color.kt
package by.riewe.cadence.ui.theme

import androidx.compose.ui.graphics.Color

// Primary — тёмно-синий (кабина грузовика)
val PrimaryDark = Color(0xFF1E3A5F)
val Primary = Color(0xFF2E4A6F)
val PrimaryLight = Color(0xFF4A6A8F)

// Secondary — оранжевый (сигнальный)
val Secondary = Color(0xFFFF6B35)
val SecondaryLight = Color(0xFFFF8A5B)
val SecondaryDark = Color(0xFFE55A2B)

// Tertiary — серо-синий
val Tertiary = Color(0xFF4A6572)
val TertiaryLight = Color(0xFF78909C)

// Фоны
val BackgroundLight = Color(0xFFF5F5F5)
val SurfaceLight = Color(0xFFFFFFFF)
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)

// Статусы
val StatusActive = Color(0xFF4CAF50)      // Зелёный — активна
val StatusClosed = Color(0xFF9E9E9E)      // Серый — закрыта
val StatusWarning = Color(0xFFFFC107)     // Жёлтый — внимание
val StatusError = Color(0xFFE53935)       // Красный — ошибка

// Текст
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF212121)
val OnSurface = Color(0xFF212121)